import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures the app is set up correctly whether
// it's running in Expo Go, in a native build, or in the web.
registerRootComponent(App);
